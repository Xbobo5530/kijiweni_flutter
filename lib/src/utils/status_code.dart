enum StatusCode { waiting, failed, success }
enum ChatMenuAction { like, share, reply, delete, report }
enum AddMenuOption{video, image, camera}
enum FileType {video, image,}
enum DetailType{name, bio, title, description, imageUrl}
enum FileUploadFor {chat, community, user}
enum Action{leave, join}
enum SourcePage{infopage, communityPage, homePage}
enum AppInfoAction{call, email, more}